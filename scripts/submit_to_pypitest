# ***************************
# *** Submit to PyPI Test *** 
# ***************************

# Fail on first error:
set -e

# Get the current directory:
OLD_DIR=$(pwd)

# Get the directory where this script is stored:
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd)"

# Change to the root directory of the skua project:
cd $DIR/..

# Register skua against PyPI's test server:
echo "Registering skua..."
python setup.py register -r pypitest

# Wait a moment:
echo "Waiting a moment before uploading..."
sleep 10

# Upload skua to PyPI's test server: 
echo "Uploading skua..."
python setup.py sdist upload -r pypitest

# Change back to the original directory:
cd $OLD_DIR
echo "Done."
